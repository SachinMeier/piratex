// HistoryFeed — alias for RecentPane to match spec naming.
// The compact 3-line history pane shown in the regular playing layout.
// Takes the same props as RecentPane and forwards them.
export { RecentPane as HistoryFeed } from "./RecentPane.js";
